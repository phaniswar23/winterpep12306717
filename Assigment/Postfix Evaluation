class Solution {
public:
    int evaluatePostfix(vector<string>& arr) {

        stack<int> st;

        for(string s : arr) {

            if(s=="+" || s=="-" || s=="*" || s=="/" || s=="^") {

                int b = st.top(); st.pop();
                int a = st.top(); st.pop();

                if(s=="+") st.push(a+b);
                else if(s=="-") st.push(a-b);
                else if(s=="*") st.push(a*b);

                else if(s=="/") {
                    double res = (double)a / b;
                    st.push(floor(res));   
                }

                else st.push(pow(a,b));
            }
            else {
                st.push(stoi(s));
            }
        }

        return st.top();
    }
};
